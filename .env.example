# Vector DB Configuration (Qdrant)
# Use QDRANT_MODE=local for offline vector database on disk
# Use QDRANT_MODE=server for Qdrant Cloud or external server instance
QDRANT_MODE=local
QDRANT_URL=
QDRANT_API_KEY=

# LLM Configuration (Groq / OpenAI / Custom Provider)
GROQ_API_KEY=
